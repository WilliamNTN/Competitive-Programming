#include <bits/stdc++.h>
using namespace std;

#define LL long long int

LL n,p;

int main(){
	cin.tie(0);
	ios_base::sync_with_stdio(0);

	cin>>n>>p;
	string entradas[n];

	for(LL i = 0; i<n; i++)
		cin>>entradas[i];

	long double sell = 0.5;
	long double total = 1;

	LL control = (n-2);
	LL exp = 0;
	


	return 0;
}
